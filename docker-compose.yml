services:
  agent:
    build:
        context: .
        dockerfile: Dockerfile
    container_name: agent
    environment:
      - CLOGS_BACKEND_URL=http://host.docker.internal:8000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
